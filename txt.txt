      let len = self.arr_expression.len()-1;
        let is_num =  self.get_last_data().parse::<f64>().is_ok();
        Calc::is_operator(&value);
        if is_num 
        {
            self.arr_expression.push(value.to_string());
            //set last number
        }else
        {
            Self::set_last_data(self, value.to_string());
            self.arr_expression.push(value);
        }